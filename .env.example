# Bearer token required on POST/write routes. Generate with:
#   node -e "console.log(require('crypto').randomBytes(24).toString('hex'))"
API_KEY=

# Optional. Omit to use the in-memory store (fine for local dev, wiped on restart).
# Railway sets this automatically when a Postgres service is attached.
DATABASE_URL=

PORT=3000
